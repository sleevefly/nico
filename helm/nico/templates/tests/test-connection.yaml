apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nico.fullname" . }}-test-connection"
  labels:
    {{- include "nico.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nico.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
